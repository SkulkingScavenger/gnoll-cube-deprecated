var output;
/*
  check if target is still valid
  must be alive
  must be vulnerable to attacks
  if in max_pursuit_range{
    if path exists{
      return true
    }else{
      if the path can't be found, probably give up.
    }
*/
return true;
